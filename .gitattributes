# Normalize all text to LF in the repo (we develop on Windows, CI runs on Linux).
* text=auto eol=lf

# Binary assets — never normalize.
*.ico binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.pdf binary
*.woff binary
*.woff2 binary
