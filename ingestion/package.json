{
  "name": "@mba-vault/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local pipeline: extract text from coursework, chunk, embed via Gemini, write the file-based vector index. Runs on your laptop, not the server.",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ingest:dryrun": "tsx scripts/dry-run.ts"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "fast-xml-parser": "^5.9.3",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "pdfjs-dist": "^6.1.200"
  }
}
